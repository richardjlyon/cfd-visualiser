---
phase: 01.1
plan: 08
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - src/charts/co2-avoided.md
  - src/charts/cumulative-subsidy.md
  - src/charts/generation-heatmap.md
autonomous: true
requirements:
  - CHART-02
  - CHART-03
  - CHART-04
  - EDIT-01
  - EDIT-02
  - EDIT-04
  - EDIT-05
  - OPS-03
  - REQ-DESIGN-08
tags: [pages, placeholders, dark-theme, tailwind]

must_haves:
  truths:
    - "Three placeholder detail pages exist at src/charts/{co2-avoided,cumulative-subsidy,generation-heatmap}.md"
    - "Each page has full editorial scaffolding: breadcrumb, h1, caption, source, download buttons, boxout"
    - "Each page renders a placeholder figure (NOT a real chart) with 'Coming in Phase 02' panel"
    - "Each page has a Download Image anchor pointing to the corresponding placeholder PNG"
    - "Each page imports /client/glossary-tooltip.js and uses at least one <abbr data-glossary> in editorial prose"
    - "All 3 pages render under npm run build without error"
  artifacts:
    - path: src/charts/co2-avoided.md
      contains: "chart-co2-avoided-placeholder.png"
      contains_2: "data-glossary"
      contains_3: "Coming in Phase 02"
    - path: src/charts/cumulative-subsidy.md
      contains: "chart-cumulative-subsidy-placeholder.png"
      contains_2: "data-glossary"
    - path: src/charts/generation-heatmap.md
      contains: "chart-heatmap-placeholder.png"
      contains_2: "data-glossary"
  key_links:
    - from: "src/charts/co2-avoided.md"
      to: "/assets/charts/chart-co2-avoided-placeholder.png"
      via: "<a download> anchor"
      pattern: "co2-avoided-placeholder"
    - from: "src/charts/cumulative-subsidy.md"
      to: "/assets/charts/chart-cumulative-subsidy-placeholder.png"
      via: "<a download> anchor"
      pattern: "cumulative-subsidy-placeholder"
    - from: "src/charts/generation-heatmap.md"
      to: "/assets/charts/chart-heatmap-placeholder.png"
      via: "<a download> anchor"
      pattern: "heatmap-placeholder"
---

<objective>
Create the three placeholder detail pages for CHART-02 (£/tCO₂ avoided), CHART-03 (Cumulative subsidy + Lorenz), and CHART-04 (Generation heatmap). Each page has the same Tailwind dark shell as the scissors page (plan 07), full editorial scaffolding, and placeholder chart bodies with downloadable placeholder PNGs. Glossary tooltip module loaded; at least one `<abbr data-glossary>` per page satisfies D-20.

Purpose: D-08 — placeholder cards route to real detail pages with full editorial scaffolding but placeholder chart bodies. Lands the dashboard *shape* now so editorial review can happen against the real layout, not against missing routes.

Output: 3 new .md files; full build succeeds; smoke test in plan 09 will assert all 3 in dist/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-CONTEXT.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-RESEARCH.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md
@src/charts/scissors.md
@src/content/captions.json
@src/content/glossary.json
</context>

<interfaces>
<!-- Page template shared across all 3 placeholders. Wave 3 plan 07 produces the scissors analog. -->

Frontmatter: `--- title: "<chart card_title>" toc: false ---`

Body sections (in order):
  1. js cell: load meta + captions
  2. Breadcrumb: ← All charts
  3. h1: chart title (text-[28px] font-semibold text-primary)
  4. Caption (EDIT-01) drawn from captions.json
  5. Intro paragraph with at least one <abbr data-glossary="slug"> term
  6. Placeholder figure: <figure class="chart" role="img" aria-labelledby aria-describedby> containing a div.bg-dashboard.rounded-sm.p-12.text-center.text-muted with "Chart coming in Phase 02 — {description}"
  7. Source line: 14px italic muted with link to LCCC dataset URL + "Data pending — chart ships in Phase 02" stamp
  8. Download strip: only the placeholder PNG (no JSON yet — that comes with the real chart in Phase 02). Label: "Download image (placeholder)" per UI-SPEC
  9. Boxout (EDIT-04): "What does this mean?" heading + body from captions.json boxout field
  10. Glossary tooltip script tag

Slug → page → glossary terms used:
  co2-avoided          → uses cfd, intermittent, dispatchable
  cumulative-subsidy   → uses cfd, allocation_round
  generation-heatmap   → uses intermittent, dispatchable
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/charts/co2-avoided.md placeholder detail page</name>
  <files>src/charts/co2-avoided.md</files>
  <read_first>
    - src/charts/scissors.md (post plan 07 — use as the structural template; keep same shell, swap chart figure for placeholder)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Detail Page Layout, Copywriting Contract → "Placeholder chart body" + "Placeholder download label" + "Empty state"; Card title + hook for CHART-02)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md (placeholder detail page section — pattern to copy + naming clarification)
    - src/content/captions.json (post plan 04 — confirm chart-co2-avoided.title, .caption, .boxout, .card_title fields)
    - src/content/glossary.json (post plan 04 — confirm cfd, intermittent, dispatchable slugs available)
  </read_first>
  <action>
    Create src/charts/co2-avoided.md:

    ```markdown
    ---
    title: "The carbon price: are CfDs worth it per tonne?"
    toc: false
    ---

    ```js
    const meta = FileAttachment("../data/meta.json").json();
    const captions = FileAttachment("../content/captions.json").json();
    ```

    <p class="text-sm text-muted mb-4">
      <a href="/" class="text-muted hover:text-accent">← All charts</a>
    </p>

    <h1 class="text-[28px] font-semibold text-primary mb-4">${captions["chart-co2-avoided"].card_title}</h1>

    <p class="text-base text-primary leading-relaxed mb-4">${captions["chart-co2-avoided"].caption}</p>

    <p class="text-base text-primary leading-relaxed mb-6">
      For each <abbr data-glossary="cfd">CfD</abbr> generator, dividing total consumer subsidy by tonnes of CO₂
      avoided gives an effective carbon price. The picture differs sharply between
      <abbr data-glossary="intermittent">intermittent</abbr> wind and solar contracts and the small number of
      <abbr data-glossary="dispatchable">dispatchable</abbr> biomass and hydro contracts.
    </p>

    <figure class="chart bg-dashboard rounded-lg overflow-hidden my-6"
            role="img"
            aria-labelledby="chart-co2-title"
            aria-describedby="chart-co2-desc">
      <div id="chart-co2-title" hidden>£/tCO₂ avoided — placeholder</div>
      <div id="chart-co2-desc" hidden>Chart coming in Phase 02</div>
      <div class="p-12 text-center text-muted">
        <p class="text-xl text-primary mb-2">Chart coming in Phase 02</p>
        <p class="text-sm">Full £/tCO₂ avoided explorer with UK ETS price and DEFRA social cost overlays.</p>
      </div>
    </figure>

    <p class="text-sm text-muted italic mt-2">
      Source: <a href="https://dp.lowcarboncontracts.uk" class="text-accent">LCCC ACGE dataset</a>
      · Data pending — chart ships in Phase 02
    </p>

    <p class="mt-2 text-sm">
      <a class="text-accent hover:underline min-h-[44px] inline-flex items-center"
         href="/assets/charts/chart-co2-avoided-placeholder.png"
         download="cfd-co2-avoided-placeholder-${meta.max_settlement_date}.png">Download image (placeholder)</a>
    </p>

    <aside class="bg-card border border-surface rounded-lg p-6 my-6">
      <h2 class="text-xl font-semibold text-primary mb-2">What does this mean?</h2>
      <div class="prose text-base text-primary leading-relaxed">
        ${captions["chart-co2-avoided"].boxout}
      </div>
    </aside>

    <script type="module" src="/client/glossary-tooltip.js"></script>
    ```

    Notes:
    - Three abbr slugs used: cfd, intermittent, dispatchable. All defined in glossary.json (test_glossary_refs.py stays GREEN).
    - PNG path matches plan 05 output filename: chart-co2-avoided-placeholder.png.
    - No Download JSON anchor — there's no real view-model yet; that ships in Phase 02.
    - Placeholder body uses "Chart coming in Phase 02" copy locked by UI-SPEC Copywriting Contract.
  </action>
  <acceptance_criteria>
    - `test -f src/charts/co2-avoided.md`
    - `grep -q 'chart-co2-avoided-placeholder.png' src/charts/co2-avoided.md`
    - `grep -q 'data-glossary="cfd"' src/charts/co2-avoided.md`
    - `grep -q 'data-glossary="intermittent"' src/charts/co2-avoided.md`
    - `grep -q 'data-glossary="dispatchable"' src/charts/co2-avoided.md`
    - `grep -q '/client/glossary-tooltip.js' src/charts/co2-avoided.md`
    - `grep -q 'Coming in Phase 02' src/charts/co2-avoided.md`
    - `grep -q '← All charts' src/charts/co2-avoided.md`
    - `grep -q 'Download image (placeholder)' src/charts/co2-avoided.md`
    - `grep -q 'min-h-\[44px\]' src/charts/co2-avoided.md` (44px tap target)
    - `grep -q 'role="img"' src/charts/co2-avoided.md` (figure ARIA)
    - `grep -q 'FileAttachment.*captions.json' src/charts/co2-avoided.md` (data wiring)
    - Build succeeds: `npm run build` exits 0
    - `(test -f dist/charts/co2-avoided/index.html || test -f dist/charts/co2-avoided.html)` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && (test -f dist/charts/co2-avoided/index.html || test -f dist/charts/co2-avoided.html)</automated>
  </verify>
  <done>
    src/charts/co2-avoided.md created with full editorial scaffolding, placeholder figure, Download Image anchor pointing to the placeholder PNG, three abbr-marked terms, glossary-tooltip script. Build succeeds; page in dist/.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/charts/cumulative-subsidy.md placeholder detail page</name>
  <files>src/charts/cumulative-subsidy.md</files>
  <read_first>
    - src/charts/co2-avoided.md (the analog from task 1 — replicate structure verbatim, swap copy + slugs + PNG filename)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Copywriting Contract — CHART-03 card_title + card_hook + boxout copy)
    - src/content/captions.json (chart-cumulative-subsidy fields)
  </read_first>
  <action>
    Create src/charts/cumulative-subsidy.md as a structural twin of co2-avoided.md, with these substitutions:

    - Front-matter title: "Cumulative subsidy: who gets the money?"
    - h1: ${captions["chart-cumulative-subsidy"].card_title}
    - Caption: ${captions["chart-cumulative-subsidy"].caption}
    - Intro paragraph (use 2 abbr slugs):
      ```html
      <p class="text-base text-primary leading-relaxed mb-6">
        Total <abbr data-glossary="cfd">CfD</abbr> consumer subsidy stacked by project, plotted across each
        <abbr data-glossary="allocation_round">allocation round</abbr>. The Lorenz curve overlay shows how
        concentrated the subsidy stream is — a small number of projects receive the majority of payments.
      </p>
      ```
    - Placeholder body description: "Cumulative consumer subsidy stacked by technology with a Lorenz concentration scatter."
    - PNG href: `/assets/charts/chart-cumulative-subsidy-placeholder.png`
    - Download filename: `cfd-cumulative-subsidy-placeholder-${meta.max_settlement_date}.png`
    - aria-labelledby/aria-describedby ids: `chart-cumulative-title` / `chart-cumulative-desc`
    - Boxout: ${captions["chart-cumulative-subsidy"].boxout}

    All other classes / structure / wording identical to co2-avoided.md.
  </action>
  <acceptance_criteria>
    - `test -f src/charts/cumulative-subsidy.md`
    - `grep -q 'chart-cumulative-subsidy-placeholder.png' src/charts/cumulative-subsidy.md`
    - `grep -q 'data-glossary="cfd"' src/charts/cumulative-subsidy.md`
    - `grep -q 'data-glossary="allocation_round"' src/charts/cumulative-subsidy.md`
    - `grep -q '/client/glossary-tooltip.js' src/charts/cumulative-subsidy.md`
    - `grep -q 'Coming in Phase 02' src/charts/cumulative-subsidy.md`
    - `grep -q '← All charts' src/charts/cumulative-subsidy.md`
    - `grep -q 'min-h-\[44px\]' src/charts/cumulative-subsidy.md`
    - `grep -q 'role="img"' src/charts/cumulative-subsidy.md`
    - Build succeeds: `npm run build` exits 0
    - `(test -f dist/charts/cumulative-subsidy/index.html || test -f dist/charts/cumulative-subsidy.html)` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && (test -f dist/charts/cumulative-subsidy/index.html || test -f dist/charts/cumulative-subsidy.html)</automated>
  </verify>
  <done>
    src/charts/cumulative-subsidy.md created with full scaffolding, placeholder figure, Download Image to chart-cumulative-subsidy-placeholder.png, abbrs for cfd + allocation_round. Build succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/charts/generation-heatmap.md placeholder detail page (note slug, NOT daily-generation)</name>
  <files>src/charts/generation-heatmap.md</files>
  <read_first>
    - src/charts/co2-avoided.md (analog)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Copywriting Contract — CHART-04 card_title + boxout)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md (NAMING NOTE: file MUST be `generation-heatmap.md`, not `daily-generation.md` — UI-SPEC and PNG filename `chart-heatmap-placeholder.png` align on the heatmap slug)
    - src/content/captions.json (chart-heatmap fields)
  </read_first>
  <action>
    Create src/charts/generation-heatmap.md (NOT daily-generation.md). Structural twin of co2-avoided.md with substitutions:

    - Front-matter title: "Intermittency: when the wind stops blowing"
    - h1: ${captions["chart-heatmap"].card_title}
    - Caption: ${captions["chart-heatmap"].caption}
    - Intro paragraph (use 2 abbr slugs):
      ```html
      <p class="text-base text-primary leading-relaxed mb-6">
        A daily generation heatmap (year × day-of-year) for wind and solar reveals when
        <abbr data-glossary="intermittent">intermittent</abbr> output collapses — the so-called Dunkelflaute periods.
        The contrast with <abbr data-glossary="dispatchable">dispatchable</abbr> technologies shows why the grid still
        depends on backup capacity.
      </p>
      ```
    - Placeholder body description: "Daily generation heatmap (year × day-of-year) for wind and solar with mobile-simplified weekly fallback."
    - PNG href: `/assets/charts/chart-heatmap-placeholder.png`  ← note **chart-heatmap**, not chart-generation-heatmap
    - Download filename: `cfd-generation-heatmap-placeholder-${meta.max_settlement_date}.png`
    - aria-labelledby/aria-describedby ids: `chart-heatmap-title` / `chart-heatmap-desc`
    - Boxout: ${captions["chart-heatmap"].boxout}

    All other classes / structure / wording identical to co2-avoided.md.

    **Slug-vs-filename mismatch note**: The page is at `/charts/generation-heatmap` but the PNG is at `/assets/charts/chart-heatmap-placeholder.png`. This is intentional per UI-SPEC Assets Inventory (the PNG drops the "generation-" prefix for brevity in the filename). Verify the filename matches what plan 05's build_all() writes.
  </action>
  <acceptance_criteria>
    - `test -f src/charts/generation-heatmap.md` (NOT daily-generation.md)
    - `! test -f src/charts/daily-generation.md` (wrong-slug file MUST NOT exist)
    - `grep -q 'chart-heatmap-placeholder.png' src/charts/generation-heatmap.md` (PNG filename per UI-SPEC)
    - `grep -q 'data-glossary="intermittent"' src/charts/generation-heatmap.md`
    - `grep -q 'data-glossary="dispatchable"' src/charts/generation-heatmap.md`
    - `grep -q '/client/glossary-tooltip.js' src/charts/generation-heatmap.md`
    - `grep -q 'Coming in Phase 02' src/charts/generation-heatmap.md`
    - `grep -q '← All charts' src/charts/generation-heatmap.md`
    - `grep -q 'min-h-\[44px\]' src/charts/generation-heatmap.md`
    - `grep -q 'role="img"' src/charts/generation-heatmap.md`
    - Build succeeds: `npm run build` exits 0
    - `(test -f dist/charts/generation-heatmap/index.html || test -f dist/charts/generation-heatmap.html)` exits 0
    - test_glossary_refs.py still GREEN: `uv run pytest tests/test_glossary_refs.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && (test -f dist/charts/generation-heatmap/index.html || test -f dist/charts/generation-heatmap.html) && uv run pytest tests/test_glossary_refs.py -x</automated>
  </verify>
  <done>
    src/charts/generation-heatmap.md created (correct slug, NOT daily-generation); placeholder figure, Download Image to chart-heatmap-placeholder.png, abbrs for intermittent + dispatchable. Build succeeds; all 3 placeholder pages now in dist/. Glossary refs test GREEN across all 3 pages.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static markdown → public HTML | Same as plan 07 — bounded to authored content |
| File-based routing → asset paths | Page slugs MUST match observablehq.config.js pages array (plan 02) and PNG filenames (plan 05); mismatches produce 404s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-16 | E (Elevation) — wrong slug breaks card link / 404 | src/charts/{slug}.md filenames | mitigate | acceptance_criteria asserts file is at `generation-heatmap.md` AND that `daily-generation.md` does NOT exist. Plan 09 smoke asserts all 4 slugs in dist/. observablehq.config.js (plan 02) registers the same 4 slugs. Triple lock. |
| T-01.1-18 | T (Tampering) — placeholder mistaken for live chart | All 3 placeholder figures | mitigate | "Chart coming in Phase 02" copy locked by UI-SPEC; Download label is "Download image (placeholder)" not "Download image"; source line says "Data pending — chart ships in Phase 02". Editorial gates against confusion. |
| T-01.1-19 | I (Information disclosure) — broken PNG href shows broken-image icon | src/charts/* href to placeholder PNGs | mitigate | PNG filenames in pages match plan 05's build_all() output literal. Plan 09 smoke asserts all 4 PNGs exist in dist/assets/charts/. |
</threat_model>

<verification>
- All 3 .md files created with correct slugs (heatmap NOT daily-generation).
- Each page references the correct placeholder PNG filename produced by plan 05.
- Each page imports glossary-tooltip and uses ≥1 valid glossary slug.
- `npm run build` succeeds; all 3 detail pages present in dist/.
- test_glossary_refs.py GREEN — all referenced slugs exist in glossary.json.
</verification>

<success_criteria>
- 3 placeholder detail pages created at the correct slugs
- Each carries full editorial scaffolding + dark Tailwind shell + placeholder figure + Download Image (placeholder PNG) + boxout + glossary tooltip
- All 3 abbr-marked terms used across the pages exist in glossary.json (test_glossary_refs.py GREEN)
- Build produces all 3 in dist/
- Plan 09 (post-build smoke + final verification) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-08-SUMMARY.md`
</output>
