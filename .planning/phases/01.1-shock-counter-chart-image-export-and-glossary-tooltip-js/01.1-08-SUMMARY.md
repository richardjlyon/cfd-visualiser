---
phase: 01.1
plan: 08
subsystem: pages
tags: [pages, placeholders, dark-theme, tailwind, glossary]
requires:
  - plan 02 (observablehq.config pages array)
  - plan 03 (Tailwind tokens / app.css)
  - plan 04 (captions.json CHART-02/03/04 fields, glossary.json slug keys)
  - plan 05 (export_chart_images.py producing placeholder PNGs)
  - plan 06 (glossary-tooltip.js module)
  - plan 07 (scissors.md shell pattern)
provides:
  - src/charts/co2-avoided.md
  - src/charts/cumulative-subsidy.md
  - src/charts/generation-heatmap.md
affects:
  - dist/charts/co2-avoided{.html,/index.html}
  - dist/charts/cumulative-subsidy{.html,/index.html}
  - dist/charts/generation-heatmap{.html,/index.html}
tech-stack:
  added: []
  patterns:
    - "Tailwind dark-theme shell pattern mirrored from plan 07 scissors.md rewrite (documented as Rule-3 deviation — parallel worktree means shells converge on merge)"
    - "Placeholder figure uses bg-dashboard rounded-lg with role=img + aria-labelledby/describedby hidden divs"
    - "Download anchor pattern: /assets/charts/{slug}-placeholder.png with download filename cfd-{slug}-placeholder-${meta.max_settlement_date}.png"
key-files:
  created:
    - src/charts/co2-avoided.md
    - src/charts/cumulative-subsidy.md
    - src/charts/generation-heatmap.md
  modified: []
decisions:
  - D-08: "Three placeholder detail pages route with full editorial scaffolding + placeholder chart body"
  - D-13/D-15: "Download image anchor uses plain <a download> (no JS) pointing to placeholder PNGs"
  - D-20: "Each page carries >=1 <abbr data-glossary> in editorial prose (not in chart figure)"
  - "Slug-vs-filename split: page at /charts/generation-heatmap but PNG at /assets/charts/chart-heatmap-placeholder.png per UI-SPEC Assets Inventory"
metrics:
  duration: ~8 min
  completed: 2026-04-15
---

# Phase 01.1 Plan 08: Placeholder detail pages for CHART-02/03/04 Summary

Three placeholder detail pages added at the correct slugs (co2-avoided, cumulative-subsidy, generation-heatmap) — each carries the Tailwind dark shell, full editorial scaffolding, a placeholder chart body, a Download image (placeholder) anchor to the corresponding plan-05 PNG, and at least one `<abbr data-glossary>` in editorial prose.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Create src/charts/co2-avoided.md placeholder detail page | `9121073` | src/charts/co2-avoided.md |
| 2 | Create src/charts/cumulative-subsidy.md placeholder detail page | `9891ed2` | src/charts/cumulative-subsidy.md |
| 3 | Create src/charts/generation-heatmap.md placeholder detail page (correct slug) | `1da856a` | src/charts/generation-heatmap.md |

## Verification

- `test -f src/charts/co2-avoided.md` — OK
- `test -f src/charts/cumulative-subsidy.md` — OK
- `test -f src/charts/generation-heatmap.md` — OK
- `! test -f src/charts/daily-generation.md` — OK (wrong-slug file absent)
- `grep` checks for `chart-{slug}-placeholder.png`, `data-glossary`, `Coming in Phase 02`, `← All charts`, `Download image (placeholder)`, `min-h-[44px]`, `role="img"`, `/client/glossary-tooltip.js`, `FileAttachment.*captions.json` — all pass
- `npm run build` — exits 0; all 5 pages (home + scissors + 3 placeholders) render
- `dist/charts/co2-avoided.html`, `dist/charts/cumulative-subsidy.html`, `dist/charts/generation-heatmap.html` — all present
- `uv run pytest tests/test_glossary_refs.py -x` — passes (5 abbr slugs used across pages: cfd, intermittent, dispatchable, allocation_round; all defined in glossary.json)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `npm install` dependencies were absent in this worktree**
- **Found during:** Task 1 verification (`npm run build`)
- **Issue:** `tailwindcss: command not found` — package.json/package-lock.json present but node_modules not installed in the worktree.
- **Fix:** Ran `npm install` to populate node_modules. `package-lock.json` was touched by the install (already tracked) — left uncommitted since it is not in this plan's scope and parallel plans (02/03) own the initial npm setup.
- **Files modified:** none tracked (node_modules/ is gitignored)
- **Commit:** n/a (environment bootstrap, not a code change)

**2. [Rule 3 - Blocking] Placeholder PNG artefacts required by Framework asset resolver**
- **Found during:** Task 1 verification (`npm run build`)
- **Issue:** Observable Framework resolves `<a href="/assets/charts/chart-co2-avoided-placeholder.png">` at build time and fails with `Not found` if the file is absent. Plan 05's `pipeline/export_chart_images.py` owns these artefacts but runs in a parallel worktree that has not merged.
- **Fix:** Ran the already-present `pipeline.export_chart_images.build_all(Path('src/assets/charts'))` to produce the real PNGs in my worktree. The PNGs remain **untracked** (they are build artefacts produced by the Python pipeline, same category as `src/assets/tailwind.css` which is gitignored). On merge with plan 05 the pipeline runs in CI and re-produces the same files.
- **Files modified:** none tracked
- **Commit:** n/a (local build artefact, not a source change)

**3. [Rule 3 - Duplication] Tailwind dark shell structure duplicated from plan 07**
- **Found during:** Task 1 authoring
- **Issue:** Plan 08 depends on plan 07's scissors.md rewrite for the shell reference, but plan 07 runs in a parallel worktree that has not merged, so `src/charts/scissors.md` in this worktree is still the Phase-01 Pico-era page.
- **Fix:** Used the UI-SPEC "Detail Page Layout" section as the source of truth for the shell (breadcrumb, h1, caption, intro prose, figure with role=img, source line, download strip, boxout, glossary-tooltip script). Post-merge the plan-07 shell and these three placeholder shells will converge on identical structural patterns by construction.
- **Files modified:** none outside plan scope
- **Commit:** n/a

No architectural changes. No auth gates. No Rule-4 questions.

## Threat Surface Check

Threat model items T-01.1-16/18/19 mitigated as planned:
- **T-01.1-16 (wrong slug)**: File named `generation-heatmap.md`, NOT `daily-generation.md`; verified by explicit `! test -f` assertion.
- **T-01.1-18 (placeholder mistaken for live chart)**: "Chart coming in Phase 02" heading, "Download image (placeholder)" label, "Data pending — chart ships in Phase 02" source stamp — all literal per UI-SPEC Copywriting Contract.
- **T-01.1-19 (broken PNG href)**: PNG filenames (`chart-co2-avoided-placeholder.png`, `chart-cumulative-subsidy-placeholder.png`, `chart-heatmap-placeholder.png`) match `pipeline/export_chart_images.build_all()` output literal; verified by successful `npm run build` resolving all `<a href>` targets.

No new threat surface introduced.

## Known Stubs

The placeholder chart bodies themselves are intentional stubs locked by D-08. Each is clearly labelled "Chart coming in Phase 02" with a one-line description of the intended chart. The real chart bodies ship in Phase 02 (CHART-02/03/04 requirements).

## Self-Check: PASSED

- Files: `src/charts/co2-avoided.md`, `src/charts/cumulative-subsidy.md`, `src/charts/generation-heatmap.md` — all FOUND
- Commits: `9121073`, `9891ed2`, `1da856a` — all FOUND in `git log`
- Build: `npm run build` exits 0; all 3 detail pages present in `dist/`
- Tests: `tests/test_glossary_refs.py` GREEN
