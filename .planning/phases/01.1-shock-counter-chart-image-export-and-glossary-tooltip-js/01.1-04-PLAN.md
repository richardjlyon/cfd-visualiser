---
phase: 01.1
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/content/glossary.json
  - src/content/captions.json
  - pipeline/editorial.py
autonomous: true
requirements:
  - REQ-GLOSS-01
  - REQ-GLOSS-02
  - REQ-GLOSS-03
  - REQ-GLOSS-04
  - REQ-GLOSS-05
  - EDIT-01
  - EDIT-05
tags: [glossary, content, editorial-grammar]

must_haves:
  truths:
    - "src/content/glossary.json uses 7 slug keys (cfd, strike_price, reference_price, imrp, allocation_round, intermittent, dispatchable)"
    - "Each glossary entry is { term: string, definition: string }"
    - "No legacy phrase keys (CfD, Strike price, Investment Contract, etc.) remain"
    - "captions.json has card_hook entries for all 4 charts; CHART-02/03/04 hooks pass EDIT-05 lint"
    - "lint_caption (or equivalent) is callable for arbitrary strings — used by tests on card hooks"
  artifacts:
    - path: src/content/glossary.json
      contains: '"cfd"'
      contains_2: '"intermittent"'
      contains_3: '"dispatchable"'
      contains_4: '"definition"'
    - path: src/content/captions.json
      contains: "card_hook"
      contains_2: "chart-co2-avoided"
      contains_3: "chart-cumulative-subsidy"
      contains_4: "chart-heatmap"
  key_links:
    - from: "src/content/glossary.json"
      to: "src/client/glossary-tooltip.js (plan 06)"
      via: "fetch and slug lookup at runtime"
      pattern: "data-glossary=\"\\w+\""
    - from: "src/content/captions.json"
      to: "src/index.md card grid (plan 07)"
      via: "FileAttachment(./content/captions.json).json() — captions[chartKey].card_hook"
      pattern: "card_hook"
---

<objective>
Migrate `src/content/glossary.json` from phrase keys to UI-SPEC slug keys (D-18), add the two missing entries `intermittent` + `dispatchable` (D-19), and extend `src/content/captions.json` with `card_hook` fields for all 4 charts (CHART-01 hook drawn from existing caption; CHART-02/03/04 hooks per UI-SPEC Copywriting Contract). Convert the Wave 0 RED tests for glossary schema and editorial card hooks to GREEN.

Purpose: The tooltip JS (plan 06) and the card grid (plan 07) both consume these JSON artefacts. Migrating both in one plan keeps the schema change atomic and the tests honest.

Output: Two JSON files migrated/extended; tests/test_glossary_schema.py + test_card_hooks_factual GREEN. Plan 06 (tooltip JS) and plan 07 (landing/scissors) unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-CONTEXT.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-RESEARCH.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md
@src/content/glossary.json
@src/content/captions.json
@pipeline/editorial.py
@tests/test_editorial_grammar.py
@tests/test_glossary_schema.py
</context>

<interfaces>
<!-- Final shapes the downstream consumers (plans 06, 07, 08) will read. -->

src/content/glossary.json (target):
```json
{
  "cfd":              { "term": "CfD",              "definition": "Contract for Difference — a government scheme that guarantees renewable generators a fixed price per MWh." },
  "strike_price":     { "term": "Strike price",     "definition": "The guaranteed £/MWh a generator is paid under its CfD." },
  "reference_price":  { "term": "Reference price",  "definition": "The market price the CfD is measured against." },
  "imrp":             { "term": "IMRP",             "definition": "Intermittent Market Reference Price — the wholesale benchmark used for wind and solar CfDs." },
  "allocation_round": { "term": "Allocation round", "definition": "A government auction round in which CfDs were awarded to generators." },
  "intermittent":     { "term": "Intermittent",     "definition": "Generators (wind, solar) whose output depends on weather and cannot be scheduled on demand." },
  "dispatchable":     { "term": "Dispatchable",     "definition": "Generators (biomass, hydro) whose output can be turned on or off at will." }
}
```

src/content/captions.json (additions — preserve existing CHART-01 entry):
```json
{
  "chart-3c": {
    "title": "<existing>",
    "caption": "<existing — CHART-01 EDIT-01 caption preserved verbatim>",
    "boxout": "<existing — preserved>",
    "card_title": "The scissors: what you pay vs the market",
    "card_hook": "Strike prices tracked above market every year except 2022."
  },
  "chart-co2-avoided": {
    "title": "The carbon price: are CfDs worth it per tonne?",
    "caption": "Many years cost £200–400 per tonne of CO₂ avoided.",
    "boxout": "Coming in Phase 02 — full £/tCO₂ avoided explorer with UK ETS price and DEFRA social cost overlays.",
    "card_title": "The carbon price: are CfDs worth it per tonne?",
    "card_hook": "Many years cost £200–400 per tonne of CO₂ avoided — above the government's own carbon value."
  },
  "chart-cumulative-subsidy": {
    "title": "Cumulative subsidy: who gets the money?",
    "caption": "Around 80% of total subsidy goes to fewer than 10 projects.",
    "boxout": "Coming in Phase 02 — cumulative consumer subsidy stacked by technology with a Lorenz concentration scatter.",
    "card_title": "Cumulative subsidy: who gets the money?",
    "card_hook": "Around 80% of total subsidy goes to fewer than 10 projects."
  },
  "chart-heatmap": {
    "title": "Intermittency: when the wind stops blowing",
    "caption": "Dunkelflaute weeks appear as voids in the daily generation grid.",
    "boxout": "Coming in Phase 02 — daily generation heatmap (year × day-of-year) for wind and solar with mobile-simplified weekly fallback.",
    "card_title": "Intermittency: when the wind stops blowing",
    "card_hook": "Dunkelflaute weeks appear as voids in the daily generation grid."
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate glossary.json to slug-keyed schema with 7 entries</name>
  <files>src/content/glossary.json</files>
  <read_first>
    - src/content/glossary.json (current phrase-keyed shape)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-RESEARCH.md (Pattern 4 — slug-keyed JSON full example with `definition` field name)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Glossary Schema Migration table — slug → term → gloss)
    - tests/test_glossary_schema.py (the RED tests this task must turn GREEN — confirm field name is `definition` not `gloss`)
  </read_first>
  <behavior>
    - File parses as a single JSON object.
    - Top-level keys are exactly the 7 slugs from interfaces (no more, no less for the shipping set; `investment_contract` from the legacy file is intentionally dropped per RESEARCH §Pattern 4 note).
    - Each value is `{ "term": <string>, "definition": <string> }`.
    - Definitions are copy from interfaces — text-only, no HTML.
    - Tests test_required_slugs, test_entry_shape, test_no_legacy_keys all GREEN.
  </behavior>
  <action>
    Replace the entire content of src/content/glossary.json with the JSON from <interfaces> §glossary.json.

    **Field name resolution**: There is a discrepancy between RESEARCH (§Pattern 4 uses `definition`) and UI-SPEC (Glossary Schema table column header uses "gloss"). RESEARCH is canonical for code-level contracts; tests in plan 01 assert `definition`. Use **`definition`** throughout.

    **Investment Contract removed**: The current glossary has an "Investment Contract" entry that does NOT appear in the 7 slugs (per UI-SPEC table + RESEARCH §Pattern 4 explicit note: "should NOT be carried over to the slug-keyed glossary"). Drop it. If editorial later finds they need it, add as `investment_contract` with team approval — out of scope for this task.

    **Definition copy**: Use the exact strings from <interfaces>. They are word-for-word from RESEARCH §Pattern 4. Do not improvise — these are canonical per the user-locked decisions D-18/D-19.

    Write the file with one entry per line for readability; the build pipeline does not require minified JSON for content files (compactness is only needed for data artefacts).

    **Verify**: After writing, run `uv run python -c "import json; data = json.load(open('src/content/glossary.json')); print(sorted(data.keys()))"` and confirm output is exactly:
    `['allocation_round', 'cfd', 'dispatchable', 'imrp', 'intermittent', 'reference_price', 'strike_price']`

    Run `uv run pytest tests/test_glossary_schema.py -x` and confirm all three tests GREEN.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import json; print(sorted(json.load(open('src/content/glossary.json')).keys()))"` outputs exactly `['allocation_round', 'cfd', 'dispatchable', 'imrp', 'intermittent', 'reference_price', 'strike_price']`
    - `uv run python -c "import json; d = json.load(open('src/content/glossary.json')); print(all(isinstance(v, dict) and 'term' in v and 'definition' in v for v in d.values()))"` outputs `True`
    - `! grep -q '"CfD":' src/content/glossary.json` (legacy phrase key gone)
    - `! grep -q '"Investment Contract":' src/content/glossary.json` (dropped per RESEARCH note)
    - `! grep -q '"gloss":' src/content/glossary.json` (field is `definition`, not `gloss`)
    - `grep -q '"intermittent":' src/content/glossary.json` (D-19 entry present)
    - `grep -q '"dispatchable":' src/content/glossary.json` (D-19 entry present)
    - `uv run pytest tests/test_glossary_schema.py -x` exits 0 (3 tests GREEN)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_glossary_schema.py -x</automated>
  </verify>
  <done>
    glossary.json contains exactly 7 slug-keyed entries with {term, definition} shape. Legacy phrase keys and Investment Contract dropped. test_glossary_schema.py all GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend captions.json with card_hook fields for all 4 charts; ensure editorial.lint exposes lint_caption</name>
  <files>src/content/captions.json, pipeline/editorial.py</files>
  <read_first>
    - src/content/captions.json (existing CHART-01 entry — preserve verbatim; add card_title + card_hook to it; add 3 new chart entries)
    - pipeline/editorial.py (lint_caption function — confirm signature `lint_caption(text: str) -> list[str]`; if it doesn't exist or has different signature, document and adapt)
    - tests/test_editorial_grammar.py (test_card_hooks_factual — verify required field names: `card_hook`, and chart keys: `chart-co2-avoided`, `chart-cumulative-subsidy`, `chart-heatmap`)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Copywriting Contract — exact card_title and card_hook strings per chart; EDIT-05 grammar rule)
  </read_first>
  <behavior>
    - captions.json has card_hook (and card_title) on all 4 chart entries.
    - Existing CHART-01 (chart-3c) entry preserved unchanged for title/caption/boxout/source fields; card_title and card_hook ADDED.
    - 3 new entries (chart-co2-avoided, chart-cumulative-subsidy, chart-heatmap) created with title, caption, boxout, card_title, card_hook.
    - All 3 new card_hooks pass EDIT-05 factual-only lint (no framing words "waste", "scandal", "broken", "unfair", "outrage", etc. — see existing pipeline/editorial.py FRAMING_WORDS list or equivalent).
    - lint_caption is importable from pipeline.editorial.
    - test_card_hooks_factual GREEN.
  </behavior>
  <action>
    1. **Inspect pipeline/editorial.py first**. Verify `lint_caption(text: str) -> list[str]` exists. If the signature differs (e.g. `lint_caption(text) -> dict` or returns booleans), adapt the test in tests/test_editorial_grammar.py::test_card_hooks_factual to match the existing contract — DO NOT change the editorial.py public API in this plan. The test's job is to confirm card hooks are clean; whatever the API shape, equivalent assertion ("no violations") applies.

       If lint_caption does not exist at all but a related function does (e.g. `lint_text`, `find_framing_words`), import that one and adjust the test.

    2. **Inspect existing captions.json**. The current Phase 01 entry for chart-3c likely has keys like `title`, `caption`, `boxout`, `source`, `last_updated`. Preserve ALL of them. Only ADD `card_title` and `card_hook` keys to the existing chart-3c entry.

    3. **Edit src/content/captions.json**. Result must contain all 4 chart entries. For chart-3c, augment in place:
       ```json
       "chart-3c": {
         "title": "<existing — preserve>",
         "caption": "<existing — preserve>",
         "boxout": "<existing — preserve>",
         <other existing fields preserved>,
         "card_title": "The scissors: what you pay vs the market",
         "card_hook": "Strike prices tracked above market every year except 2022."
       }
       ```
       Add the 3 new entries verbatim from <interfaces>.

       Note on caption/card_hook duplication: where caption and card_hook are similar, the duplication is intentional — caption is for the chart figure, card_hook is for the landing page card. Both must independently pass EDIT-05.

    4. **EDIT-05 self-check**. Read each card_hook aloud and verify NO framing words. The hooks supplied are factual:
       - "Strike prices tracked above market every year except 2022." — factual claim, sourced.
       - "Many years cost £200–400 per tonne of CO₂ avoided — above the government's own carbon value." — factual + comparison, no loaded words.
       - "Around 80% of total subsidy goes to fewer than 10 projects." — factual concentration claim.
       - "Dunkelflaute weeks appear as voids in the daily generation grid." — descriptive observation; "void" is geometric, not pejorative.

       If editorial.py's framing word list flags any of these, EITHER (a) tweak the hook (preserve the factual claim) OR (b) verify the lint is over-broad and document. Prefer (a) — the user's editorial discipline is the locked decision.

    5. **Confirm pipeline/editorial.py exposes lint_caption** (or the equivalent function the test imports). If not exposed publicly, add to the module's `__all__` or re-export.

    6. Run `uv run pytest tests/test_editorial_grammar.py -x` and confirm all editorial tests (existing + new test_card_hooks_factual) GREEN.

    **Mitigation for editorial integrity threat (security_context T-01.1-extension)**: The card_hook lint extends the EDIT-05 enforcement surface to the landing page hooks — without it, a future contributor could slip a framing word into a hook and bypass the existing chart-caption-only lint.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import json; d=json.load(open('src/content/captions.json')); assert all(k in d for k in ['chart-3c','chart-co2-avoided','chart-cumulative-subsidy','chart-heatmap']); print('ok')"` outputs `ok`
    - `uv run python -c "import json; d=json.load(open('src/content/captions.json')); assert all('card_hook' in d[k] and d[k]['card_hook'] for k in ['chart-3c','chart-co2-avoided','chart-cumulative-subsidy','chart-heatmap']); print('ok')"` outputs `ok`
    - `grep -q '"card_title"' src/content/captions.json` (exit 0)
    - `uv run python -c "from pipeline.editorial import lint_caption; print('ok')"` outputs `ok` (function importable)
    - `uv run pytest tests/test_editorial_grammar.py -x` exits 0 (all editorial tests GREEN, including new test_card_hooks_factual)
    - Existing chart-3c title/caption/boxout fields unchanged (manually inspected — diff shows additions only, no deletions)
    - `git diff src/content/captions.json` shows only additions — zero key deletions from existing chart-3c entry (boxout, source, caption fields preserved)
    - `uv run python -m json.tool src/content/captions.json > /dev/null` (exit 0 — file is valid JSON)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m json.tool src/content/captions.json > /dev/null && uv run pytest tests/test_editorial_grammar.py -x</automated>
  </verify>
  <done>
    captions.json has chart-3c (preserved + augmented) and 3 new chart entries, each with card_title + card_hook. lint_caption importable from pipeline.editorial. All editorial tests GREEN including the card_hook lint extension.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static JSON content → DOM | glossary.json + captions.json values rendered into pages and tooltips |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | T (Tampering) — XSS via glossary content | src/content/glossary.json definition strings | mitigate (via plan 06) | This plan ensures definitions are plain text strings (no HTML markup committed). Plan 06 (tooltip JS) ensures rendering uses textContent. Defence-in-depth: even if plan 06 regressed to innerHTML, this plan's content is safe as long as no maintainer pastes HTML into glossary.json — ongoing discipline guarded by tests in plan 06. |
| T-01.1-10 | I (Information disclosure) — editorial framing leak | src/content/captions.json card_hook fields | mitigate | EDIT-05 lint extension (test_card_hooks_factual) blocks framing words in card hooks. Editorial integrity preserved — the project's credibility threat per CLAUDE.md ("Credibility loss from one bad chart is expensive"). |
</threat_model>

<verification>
- glossary.json: 7 entries, slug-keyed, {term, definition} shape, no legacy keys. test_glossary_schema all GREEN.
- captions.json: 4 chart entries, card_hook on each, EDIT-05 clean. test_editorial_grammar all GREEN including new test_card_hooks_factual.
- Both files parse as valid JSON (`python -m json.tool`).
- No regression in existing chart-3c content.
- test_glossary_refs.py remains GREEN (no .md file references any abbr data-glossary slugs yet — this changes when plans 07/08 ship pages).
</verification>

<success_criteria>
- glossary.json migrated to slug schema with all 7 entries, including new intermittent + dispatchable
- captions.json has card_hook fields on all 4 charts, all EDIT-05 clean
- lint_caption importable from pipeline.editorial
- All Wave 0 RED tests in test_glossary_schema.py and test_editorial_grammar.py::test_card_hooks_factual now GREEN
- Plans 06 (tooltip), 07 (landing/scissors), 08 (placeholder pages) unblocked — they have content to render
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-04-SUMMARY.md`
</output>
