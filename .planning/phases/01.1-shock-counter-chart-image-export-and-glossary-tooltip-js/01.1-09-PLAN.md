---
phase: 01.1
plan: 09
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - tests/post_build_smoke.sh
autonomous: false
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - EDIT-05
  - REQ-VERIFY-01
tags: [verification, smoke-test, full-build, checkpoint]

must_haves:
  truths:
    - "A clean full-build (npm run clean && npm run build) succeeds end-to-end including CSS + Framework + pipeline-emitted JSON/PNG"
    - "post_build_smoke.sh exits 0 — all dist/ artefacts present and correct"
    - "Full pytest + node --test + smoke shell suite is GREEN"
    - "No references to deferred behaviours (Phase 02 charts, methodology page, dark-mode toggle, multi-year shock variants) leaked into shipped code"
    - "User has visually confirmed the dark dashboard, ticking shock counter, working glossary tooltip on at least one page"
  artifacts:
    - path: tests/post_build_smoke.sh
      provides: "Final assertions: 4 chart pages, 4 PNGs, tailwind.css, no pico, glossary.json reachable, shock-numeral present"
  key_links:
    - from: "tests/post_build_smoke.sh"
      to: "dist/"
      via: "fail() on first missing artefact"
      pattern: "fail.*missing"
---

<objective>
Run the full Wave-4 verification: clean build, full pytest + Node + smoke suite, manual visual checkpoint with the user. Adjust the post_build_smoke.sh script if path conventions encountered during the real build differ from the Wave 0 assumptions (e.g. Framework writes /charts/scissors.html vs /charts/scissors/index.html depending on cleanUrls config). This is the gate plan — exit conditions trigger the user-visible checkpoint.

Purpose: D-16 + OPS-02 enforce that a failing build leaves the previous deployment intact. Phase 01.1 needs an explicit pass on the full smoke suite before the next push to main triggers Cloudflare deploy. The user-visual checkpoint also catches the manual UX subjectives (D-10 "money flowing" feel, D-01 "matches energydashboard.co.uk" closeness) that automated tests cannot.

Output: Final pass; Phase 01.1 ready to ship. SUMMARY captures any path-convention adjustments made to post_build_smoke.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-CONTEXT.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-VALIDATION.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md
@tests/post_build_smoke.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full clean-build + verify smoke suite; adjust smoke paths if Framework convention differs</name>
  <files>tests/post_build_smoke.sh</files>
  <read_first>
    - tests/post_build_smoke.sh (current shape from plan 01)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-VALIDATION.md (Full suite command, Per-Task Verification Map — every row should now be ✅)
    - dist/ directory listing after first full build (you'll inspect this dynamically)
  </read_first>
  <action>
    1. **Clean build**:
       ```
       npm run clean
       uv run python -m pipeline   # or whatever the existing CLI entry is — confirm against pipeline/__main__.py
       npm run build
       ```
       Confirm exit 0 from each step.

       The `uv run python -m pipeline` step regenerates src/data/*.json, src/data/meta.json, AND src/assets/charts/*.png (plan 05's Step 5b). If the test fixtures already provide src/data/chart-3c.json from Phase 01, the pipeline run is still needed to refresh meta.json with the 4 new fields and to produce the 4 dark-themed PNGs.

    2. **Inspect dist/ structure**:
       ```
       find dist -type f | head -50
       ```
       Look specifically for:
       - dist/index.html
       - dist/charts/scissors/index.html OR dist/charts/scissors.html (Framework path convention)
       - dist/charts/{co2-avoided,cumulative-subsidy,generation-heatmap}/{index.html or .html}
       - dist/assets/tailwind.css
       - dist/assets/charts/chart-3c.png + 3 placeholders
       - dist/content/glossary.json OR dist/_file/content/glossary.json (Framework asset path)

       If Framework writes glossary.json to /_file/content/glossary.json, the page client-side fetch in glossary-tooltip.js (which fetches /content/glossary.json) will 404. **Two fixes**:
         (a) Update glossary-tooltip.js to fetch /_file/content/glossary.json — but Observable Framework provides the FileAttachment URL helper for this; ideally pages should resolve the URL via Framework's mechanism and pass it to the module. For Phase 01.1 simplicity, a pragmatic alternative:
         (b) Add an explicit copy/symlink rule via observablehq.config.js `static` configuration so /content/glossary.json is served at the literal path the module fetches.

       Inspect the actual dist/ layout first; only intervene if the fetch path is broken.

    3. **Run full test suite**:
       ```
       uv run pytest -x
       node --test tests/glossary-tooltip.test.mjs tests/shock-counter.test.mjs
       bash tests/post_build_smoke.sh
       ```
       All three must exit 0.

    4. **If post_build_smoke.sh fails on path conventions**, adjust the script:
       - Either the script's fallback `[ -f "$DIST/charts/$slug/index.html" ] || [ -f "$DIST/charts/$slug.html" ]` already covers both conventions — this should pass unconditionally.
       - If glossary.json is at a different path, update the script's glossary-reachable assertion accordingly.
       - Document any change in the SUMMARY.

    5. **Editorial grammar full sweep**:
       ```
       uv run pytest tests/test_editorial_grammar.py -v
       ```
       Confirms test_card_hooks_factual still GREEN (regression check after pages shipped — no new hooks introduced violating EDIT-05).

    6. **Commit any smoke-script adjustments** with message `test(01.1): adjust post_build_smoke for actual Framework dist conventions` — only if changes were necessary.
  </action>
  <acceptance_criteria>
    - `npm run clean && uv run python -m pipeline && npm run build` exits 0 end-to-end
    - `uv run pytest -x` exits 0 (full Python suite GREEN)
    - `node --test tests/glossary-tooltip.test.mjs tests/shock-counter.test.mjs` exits 0
    - `bash tests/post_build_smoke.sh` exits 0
    - `find dist -name 'chart-3c.png'` returns at least one path
    - `find dist -name 'tailwind.css'` returns exactly one path
    - `find dist -name '*placeholder.png'` returns 3 paths (co2-avoided, cumulative-subsidy, heatmap)
    - `! find dist -name '*.css' -exec grep -l pico {} \;` returns nothing (no Pico leak)
    - `find dist -name 'index.html' | wc -l` returns ≥ 5 (landing + 4 detail pages, possibly more)
    - `grep -q 'shock-numeral' dist/index.html` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run clean && uv run python -m pipeline && npm run build && uv run pytest -x && node --test tests/glossary-tooltip.test.mjs tests/shock-counter.test.mjs && bash tests/post_build_smoke.sh</automated>
  </verify>
  <done>
    Full clean-build + full test suite + post-build smoke all GREEN. Any smoke-script path adjustments committed. dist/ contains landing + 4 detail pages + 4 PNGs + tailwind.css + glossary.json (at the path the tooltip module can fetch).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual checkpoint — confirm dark theme, ticking shock counter, working tooltip</name>
  <what-built>
    A full dark-themed dashboard:
    - Landing page at / with hero "shock counter" (ticks upward continuously, frozen if you have OS-level reduced-motion enabled) and a 2×2 card grid linking to 4 detail pages.
    - Scissors detail page at /charts/scissors with a real interactive chart, dark palette, "Download image" + "Download JSON" buttons, glossary tooltips on key terms (CfD, strike price, etc.).
    - 3 placeholder detail pages at /charts/{co2-avoided, cumulative-subsidy, generation-heatmap} with full editorial scaffolding and "Coming in Phase 02" placeholder figures, each with a Download Image (placeholder) anchor.

    The full pipeline + build + smoke suite has run GREEN. This checkpoint covers the subjective UX claims that automated tests cannot:
    - D-10 "feels like money flowing" — does the counter drift at a believable, readable rate?
    - D-01 / D-04 "matches energydashboard.co.uk/live aesthetic" — is the visual closeness adequate?
    - D-08 "placeholders read as 'coming soon' not 'broken'" — do the 3 placeholder pages feel intentional?
    - D-17 hover delay (150ms) — does the tooltip feel responsive without flickering on mouse pass-through?
    - General accessibility — focus rings visible? Tap targets feel right on mobile?
  </what-built>
  <how-to-verify>
    1. Start the dev server:
       ```
       npm run dev
       ```
       Visit http://localhost:3000 (or whatever port observable preview reports).

    2. **Landing page checks**:
       - Page loads with dark navy background and light text — no light-mode flash.
       - Hero band shows "Total CfD subsidy paid since 1 Jan {year}" + a large blue numeral (£N.NN billion) + "As of YYYY-MM-DD" stamp.
       - Watch the numeral for ~30 seconds: confirm monotonic upward drift at a readable rate (D-10). It should look "fast enough to feel alive, slow enough to read".
       - 4 cards visible in 2×2 layout on desktop (resize window to confirm 1×4 stacking on mobile).
       - Each card has a title, a one-line hook below it, and "Explore →" hint at the bottom.
       - Hover a card: border turns blue accent.
       - Tab through the page: focus ring is visible (blue outline) on every interactive element.

    3. **Reduced-motion check (optional but recommended)**:
       - macOS: System Settings → Accessibility → Display → Reduce motion (toggle on).
       - Reload landing page: counter should be FROZEN at the build-time value (no upward drift). Confirm.
       - Disable reduce-motion afterwards.

    4. **Scissors detail page checks**:
       - Click the Scissors card → /charts/scissors loads.
       - Breadcrumb "← All charts" present at top, links back to /.
       - Chart renders with dark plot area, blue strike line, orange market line, red+green area fills.
       - Round-toggle radio works (toggles between All / Investment Contract / AR1 / etc.).
       - "Download image" and "Download this chart's data (JSON)" both visible below the chart with `·` separator.
       - Click "Download image" → confirm a chart-3c.png file downloads (filename includes the build date).
       - Hover a `<abbr>` term in the intro paragraph (CfD, strike price, etc.): tooltip appears after ~150ms with the term + definition. Mouse off: tooltip closes after ~100ms.
       - Tab to a `<abbr>` and press Enter: tooltip opens. Press Esc: closes.
       - "What does this mean?" boxout visible below source line.

    5. **Placeholder pages check**:
       - Visit each of /charts/co2-avoided, /charts/cumulative-subsidy, /charts/generation-heatmap.
       - Each page shows: breadcrumb, h1, caption, intro paragraph, placeholder figure ("Chart coming in Phase 02"), source line ("Data pending..."), "Download image (placeholder)" anchor, boxout.
       - Click the placeholder Download Image: confirm a dark-themed placeholder PNG downloads.
       - Tooltip works on each page (hover an `<abbr>` term).

    6. **Aesthetic comparison**:
       - Open https://www.energydashboard.co.uk/live in a separate tab.
       - Compare card backgrounds, text colour, chart line saturation against the local site.
       - Decide: is the visual closeness adequate per D-01? If not, identify the 1-2 most important deltas (e.g. card border too prominent, text too muted) and request follow-up adjustments via /gsd-quick.

    7. **Mobile sanity** (optional, if a phone is handy or use browser DevTools mobile emulator):
       - Set viewport to 375px wide.
       - Cards stack 1×4. Shock counter numeral resizes down.
       - Tap targets feel comfortable (44×44 minimum enforced by Tailwind classes).

    Stop the dev server (Ctrl+C) once verification is complete.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — Phase 01.1 ships as-is. The /gsd-execute-phase orchestrator records the manual checkpoints from VALIDATION.md as PASS.
    - "issues: ..." — describe what to fix. Most likely candidates: shock counter rate too slow/fast (rolling-window adjustment in build_meta.py), card aesthetic too far from reference (Tailwind colour token tweaks in app.css), placeholder copy reads as broken (captions.json edit). Spawn /gsd-quick for each.
    - "blocked: ..." — flag a build failure or test regression that the automated suite missed.
  </resume-signal>
  <action>Pause execution and present the verification steps above to the user. Do not proceed past this checkpoint until the user replies with "approved", "issues: ...", or "blocked: ...". The orchestrator records the resume signal in the phase verification log.</action>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev server → developer browser | Localhost-only; no production exposure |
| Built dist/ → forthcoming Cloudflare deploy | Phase 01 plan 05 already covers the deploy pipeline; this plan only verifies the artefact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-20 | T (Tampering) — broken build deploys silently | full pipeline + build sequence | mitigate | npm run clean + full pipeline + full build + full test suite + smoke MUST all GREEN before this plan returns success. Phase 01 plan 05's CI workflow already gates Cloudflare deploy on test pass — this plan confirms the same gates apply locally. |
| T-01.1-21 | I (Information disclosure) — UX subjective failure ships as-is | manual checkpoint | mitigate | checkpoint:human-verify pauses for the user to subjectively confirm D-10 / D-01 / D-08 — claims that automated tests cannot evaluate. The /gsd-execute-phase orchestrator records the resume signal. |
| T-01.1-22 | I — analytics token leak in dist | observablehq.config.js (preserved from Phase 01) | accept | Phase 01 plan 05 owns the analytics provisioning; this plan does not alter it. If a placeholder __CF_WEB_ANALYTICS_TOKEN__ string appears in dist/index.html, it is a Phase 01 follow-up, not a Phase 01.1 regression. |
</threat_model>

<verification>
- Full automated suite GREEN: pytest, node --test, post_build_smoke.sh.
- dist/ contains all expected artefacts (5 HTML pages, 4 PNGs, tailwind.css, glossary.json reachable).
- Manual checkpoint resume-signal recorded by orchestrator.
- All Per-Task Verification Map rows in 01.1-VALIDATION.md flip from ⬜ pending to ✅ green.
</verification>

<success_criteria>
- Clean build + full pipeline + full test suite + smoke all GREEN
- post_build_smoke.sh adjusted (if necessary) to match actual Framework path conventions; any change committed
- User has visually verified the live dev preview and approved (or filed specific follow-ups)
- Phase 01.1 is shippable
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-09-SUMMARY.md`. Include:
- Any post_build_smoke.sh adjustments made (with rationale)
- The user's resume signal verbatim
- Final state of the Per-Task Verification Map (all ✅, or list of any ⚠️/❌ deferred to a follow-up plan)
</output>
