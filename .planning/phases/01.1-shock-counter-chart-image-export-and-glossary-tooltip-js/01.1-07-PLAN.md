---
phase: 01.1
plan: 07
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - src/index.md
  - src/charts/scissors.md
autonomous: true
requirements:
  - CHART-01
  - EDIT-01
  - EDIT-02
  - EDIT-04
  - EDIT-05
  - OPS-03
  - REQ-DESIGN-01
  - REQ-DESIGN-02
  - REQ-DESIGN-04
  - REQ-DESIGN-05
  - REQ-DESIGN-06
  - REQ-SHOCK-01
  - REQ-SHOCK-02
tags: [pages, landing, scissors, dark-theme, tailwind]

must_haves:
  truths:
    - "src/index.md renders nav + hero shock counter band + 4-card grid + footer"
    - "src/charts/scissors.md retains data wiring (FileAttachment chart-3c.json + meta.json + captions.json) but uses Tailwind-utility shell with dark theme"
    - "Both pages load /client/shock-counter.js (landing only) and /client/glossary-tooltip.js (both)"
    - "Scissors page has Download Image (chart-3c.png) + Download JSON anchors"
    - "Editorial prose in both pages wraps key terms in <abbr data-glossary='slug'> using slugs from the migrated glossary.json"
    - "Plot call uses dark-theme tokens (#58a6ff strike, #e6a817 market, #f44336/0.20 subsidy fill, #4caf50/0.20 clawback fill, #0d1117 background)"
  artifacts:
    - path: src/index.md
      contains: 'id="shock-numeral"'
      contains_2: "data-ytd"
      contains_3: "data-rate"
      contains_4: "/client/shock-counter.js"
      contains_5: "/client/glossary-tooltip.js"
      contains_6: "card"
    - path: src/charts/scissors.md
      contains: "chart-3c.json"
      contains_2: "/assets/charts/chart-3c.png"
      contains_3: "data-glossary"
      contains_4: "#58a6ff"
      contains_5: "/client/glossary-tooltip.js"
  key_links:
    - from: "src/index.md"
      to: "#shock-numeral + /client/shock-counter.js"
      via: "data attributes baked from meta.json by Framework reactive cell"
      pattern: "data-ytd=\"\\$\\{meta\\.ytd_subsidy_gbp\\}\""
    - from: "src/index.md card grid"
      to: "src/charts/{slug}.md detail pages"
      via: "<a href='./charts/...'>"
      pattern: "href=\"\\./charts/(scissors|co2-avoided|cumulative-subsidy|generation-heatmap)\""
    - from: "src/charts/scissors.md"
      to: "/assets/charts/chart-3c.png"
      via: "<a download> anchor"
      pattern: "download.*chart-3c"
---

<objective>
Rewrite the landing page (src/index.md) to the new dark dashboard layout (hero shock counter + 4-card grid + footer) and rewrite the scissors page shell (src/charts/scissors.md) to the new Tailwind-utility dark theme. Preserve the data-loader wiring on the scissors page (D-decisions explicit). This is the high-visibility user-facing payoff plan.

Purpose: D-05/D-06/D-07 (landing structure + 4 cards). D-14 ties the scissors page to the real PNG download. The landing page hero is where the shock counter (D-09..D-12) becomes visible. Glossary tooltips (D-17..D-20) attach via `<abbr data-glossary>` markup added throughout editorial prose.

Output: 2 page files rewritten (one new shell, one preserved data wiring + new shell); both render under `npm run build` without errors and pass post_build_smoke.sh assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-CONTEXT.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-RESEARCH.md
@.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md
@src/index.md
@src/charts/scissors.md
@src/content/glossary.json
@src/content/captions.json
</context>

<interfaces>
<!-- Data the pages consume — produced by plans 03 (meta.json fields), 04 (captions.json + glossary.json), 05 (PNGs). -->

meta.json (read by both pages):
  - ytd_subsidy_gbp:   number
  - ytd_as_of:         "YYYY-MM-DD"
  - gbp_per_sec_rate:  number
  - ytd_label_year:    number
  - max_settlement_date: "YYYY-MM-DD"
  - last_updated:      ISO timestamp

captions.json (read by both pages):
  - chart-3c.{title, caption, boxout, card_title, card_hook, ...}
  - chart-co2-avoided.{title, card_title, card_hook, ...}
  - chart-cumulative-subsidy.{...}
  - chart-heatmap.{...}

Glossary slugs available for `<abbr data-glossary="slug">` markup:
  cfd, strike_price, reference_price, imrp, allocation_round, intermittent, dispatchable

Asset paths:
  /assets/charts/chart-3c.png   (built by plan 05)
  /assets/tailwind.css          (built by plan 02)
  /client/shock-counter.js      (created in plan 06)
  /client/glossary-tooltip.js   (created in plan 06)
  /content/glossary.json        (migrated in plan 04 — fetched by tooltip module)
  /data/chart-3c.json           (existing — Phase 01)
  /data/meta.json               (extended in plan 03)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite src/index.md as landing page (nav + shock counter hero + 4-card grid + footer)</name>
  <files>src/index.md</files>
  <read_first>
    - src/index.md (current minimal placeholder — replace fully)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Landing Page Layout — Hero, Card Grid, Page Structure tables; Copywriting Contract — exact card titles/hooks; Responsive Breakpoints)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md (src/index.md section — data-loader pattern + new structure + script tags)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-RESEARCH.md (Pattern 2 example markup for hero shock counter)
    - src/charts/scissors.md (lines 8-13 — data-loader pattern to mirror)
    - src/content/captions.json (post plan 04 — confirm card_title and card_hook field names)
  </read_first>
  <action>
    Replace src/index.md entirely. The file uses Observable Framework Markdown with embedded js cells.

    Structure (per UI-SPEC §Landing Page Layout):
    ```markdown
    ---
    title: "CfD Visualiser — what the UK's low-carbon contracts really cost"
    toc: false
    ---

    ```js
    const meta = FileAttachment("./data/meta.json").json();
    const captions = FileAttachment("./content/captions.json").json();
    ```

    <header class="bg-card border-b border-surface">
      <div class="max-w-7xl mx-auto px-4 py-4">
        <a href="/" class="text-xl font-semibold text-primary no-underline">CfD Visualiser</a>
      </div>
    </header>

    <section class="bg-card py-12 sm:py-16 px-4 text-center border-b border-surface">
      <p class="text-base text-muted mb-2">Total CfD subsidy paid since 1 Jan ${meta.ytd_label_year}</p>
      <p class="text-shock text-accent mb-2"
         id="shock-numeral"
         data-ytd="${meta.ytd_subsidy_gbp}"
         data-rate="${meta.gbp_per_sec_rate}"
         aria-live="off">£${(meta.ytd_subsidy_gbp / 1e9).toFixed(2)} billion</p>
      <p class="text-sm text-muted">As of ${meta.ytd_as_of}</p>
    </section>

    <section class="max-w-7xl mx-auto px-4 py-12">
      <h2 class="text-xl font-semibold text-primary mb-6">Explore the charts</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">

        <a class="card bg-card border border-surface rounded-lg p-6 min-h-[44px] no-underline"
           href="./charts/scissors">
          <h3 class="text-xl font-semibold text-primary mb-2">${captions["chart-3c"].card_title}</h3>
          <p class="text-sm text-muted mb-4">${captions["chart-3c"].card_hook}</p>
          <span class="text-sm text-muted">Explore →</span>
        </a>

        <a class="card bg-card border border-surface rounded-lg p-6 min-h-[44px] no-underline"
           href="./charts/co2-avoided">
          <h3 class="text-xl font-semibold text-primary mb-2">${captions["chart-co2-avoided"].card_title}</h3>
          <p class="text-sm text-muted mb-4">${captions["chart-co2-avoided"].card_hook}</p>
          <span class="text-sm text-muted">Explore →</span>
        </a>

        <a class="card bg-card border border-surface rounded-lg p-6 min-h-[44px] no-underline"
           href="./charts/cumulative-subsidy">
          <h3 class="text-xl font-semibold text-primary mb-2">${captions["chart-cumulative-subsidy"].card_title}</h3>
          <p class="text-sm text-muted mb-4">${captions["chart-cumulative-subsidy"].card_hook}</p>
          <span class="text-sm text-muted">Explore →</span>
        </a>

        <a class="card bg-card border border-surface rounded-lg p-6 min-h-[44px] no-underline"
           href="./charts/generation-heatmap">
          <h3 class="text-xl font-semibold text-primary mb-2">${captions["chart-heatmap"].card_title}</h3>
          <p class="text-sm text-muted mb-4">${captions["chart-heatmap"].card_hook}</p>
          <span class="text-sm text-muted">Explore →</span>
        </a>

      </div>
    </section>

    <footer class="bg-card border-t border-surface mt-12 py-8 px-4">
      <div class="max-w-7xl mx-auto text-sm text-muted">
        Built daily from the LCCC dataset.
        · Source: <a href="https://dp.lowcarboncontracts.uk" class="text-accent">dp.lowcarboncontracts.uk</a>
        · Last build: ${meta.last_updated}
      </div>
    </footer>

    <script type="module" src="/client/shock-counter.js"></script>
    <script type="module" src="/client/glossary-tooltip.js"></script>
    ```

    **Notes on Observable Framework idioms**:
    - The `js` fenced block at top declares reactive variables. The `${...}` interpolations inside HTML refer to those variables.
    - Card titles + hooks come from captions.json fields per plan 04.
    - The `aria-live="off"` on shock-numeral is per UI-SPEC — ticking is decorative noise for screen readers; static label and as-of stamp provide accessible reading.
    - The card hrefs use `./charts/{slug}` matching D-21 file-based routing. Plan 08 creates the placeholder pages at the same slugs — `co2-avoided`, `cumulative-subsidy`, `generation-heatmap`. **NOT** `daily-generation`.
    - No `<abbr data-glossary>` markup needed on the landing page in this minimal version — there is no editorial prose using glossary terms here. The tooltip script still loads (zero overhead, no abbr → no listeners attached). This satisfies D-20 ("editorial prose on all pages") trivially since no editorial prose with technical terms exists on landing.

    **Mitigation note for STRIDE-T (T-01.1-02)**: The "As of" text uses `meta.ytd_as_of` (data date), not `meta.last_updated` (build time). The label "Total CfD subsidy paid since 1 Jan {ytd_label_year}" derives the year from the data, not from `today` — preventing a January-1st rollover where label could mis-match the underlying data window.

    Run `npm run build:css && npx @observablehq/framework build` and confirm:
    - dist/index.html produced
    - Contains `id="shock-numeral"`
    - Contains links to /client/shock-counter.js and /client/glossary-tooltip.js
    - Card hrefs present
  </action>
  <acceptance_criteria>
    - `grep -q 'id="shock-numeral"' src/index.md`
    - `grep -q 'data-ytd' src/index.md && grep -q 'data-rate' src/index.md`
    - `grep -q '/client/shock-counter.js' src/index.md`
    - `grep -q '/client/glossary-tooltip.js' src/index.md`
    - `grep -q './charts/scissors' src/index.md`
    - `grep -q './charts/co2-avoided' src/index.md`
    - `grep -q './charts/cumulative-subsidy' src/index.md`
    - `grep -q './charts/generation-heatmap' src/index.md` (NOT daily-generation)
    - `grep -q 'card_title' src/index.md && grep -q 'card_hook' src/index.md` (captions.json fields wired)
    - `grep -q 'meta.ytd_subsidy_gbp' src/index.md`
    - `grep -q 'meta.ytd_as_of' src/index.md`
    - `grep -q 'meta.ytd_label_year' src/index.md`
    - `grep -q 'aria-live="off"' src/index.md` (UI-SPEC accessibility)
    - `grep -q 'max-w-7xl' src/index.md` (UI-SPEC content column)
    - `grep -q 'sm:grid-cols-2' src/index.md` (responsive grid)
    - Build succeeds: `npm run build` exits 0 (this also covers css:build and observable build)
    - dist/index.html exists and `grep -q 'id="shock-numeral"' dist/index.html` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q 'id="shock-numeral"' dist/index.html</automated>
  </verify>
  <done>
    src/index.md rewritten with nav + hero shock counter + 4-card grid + footer; uses Tailwind utility classes throughout; imports both client modules; cards link to all 4 detail pages with corrected `generation-heatmap` slug; wired to meta.json shock-counter fields and captions.json card_title/card_hook; build succeeds; dist/index.html contains shock-numeral element.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite src/charts/scissors.md page shell (preserve data wiring, add Download Image, dark-theme Plot)</name>
  <files>src/charts/scissors.md</files>
  <read_first>
    - src/charts/scissors.md (full current file — preserve every FileAttachment line, the round-toggle pattern, the figure ARIA wrapping; rewrite ONLY visual classes + add Download Image + glossary abbr markup)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-UI-SPEC.md (Detail Page Layout table, Per-Chart Download Image Button table, Glossary Tooltip Component table, Copywriting Contract for breadcrumb + boxout + download labels)
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-PATTERNS.md (src/charts/scissors.md section — explicit list of "preserve" vs "re-skin" with line references; New additions for 01.1 list)
    - .planning/phases/01-pipeline-first-chart/01-UI-SPEC.md (download-image anchor pattern still canonical from Phase 01)
    - src/content/glossary.json (post plan 04 — confirm available slugs for editorial prose markup)
  </read_first>
  <action>
    Read src/charts/scissors.md in full first. Identify:
    - The `js` fenced cell containing FileAttachment data loaders (lines ~8-13).
    - The `roundLabels` const + `Inputs.radio` view (lines ~24-30).
    - The `<figure class="chart" role="img" aria-labelledby ...>` wrapping.
    - The Plot call with `Plot.areaY`, two `Plot.lineY`, `Plot.ruleY([0])`.
    - The download-JSON anchor (lines ~128-129).
    - The boxout `<aside>` and source-line markup.

    **Preserve verbatim**:
    - All FileAttachment loader lines + chartDataUrl.
    - The round-toggle Inputs.radio.
    - The Plot mark structure (areaY, lineY, lineY, ruleY) — only swap colours.
    - The figure ARIA contract (role="img", aria-labelledby, aria-describedby, hidden title div, figcaption).
    - The Download JSON anchor.

    **Rewrite**:
    - Page front-matter: keep `--- title: "..." toc: false ---`. Update title to use the chart-3c card_title (or keep existing — check captions.json).
    - Top of body: add `← All charts` breadcrumb (text-sm text-muted hover:text-accent anchor to /).
    - h1: 28px/600/text-primary (Tailwind class `text-[28px] font-semibold text-primary`).
    - Caption (EDIT-01): `text-base text-primary` paragraph.
    - Figure: `figure.chart` (CSS class from app.css already provides bg-dashboard rounded-lg).
    - Source line (EDIT-02): `text-sm text-muted italic` with link to LCCC dataset URL + last_updated stamp.
    - Download strip: inline block below source line containing both anchors separated by `·`:
      ```html
      <p class="mt-2 text-sm">
        <a class="text-accent hover:underline min-h-[44px] inline-flex items-center"
           href="${chartDataUrl}" download="chart-3c.json">Download this chart's data (JSON)</a>
        ·
        <a class="text-accent hover:underline min-h-[44px] inline-flex items-center"
           href="/assets/charts/chart-3c.png" download="cfd-scissors-${meta.max_settlement_date}.png">Download image</a>
      </p>
      ```
    - Boxout (EDIT-04): `<aside class="bg-card border border-surface rounded-lg p-6 my-6"><h2 class="text-xl font-semibold text-primary">What does this mean?</h2><div class="prose text-base text-primary leading-relaxed">${captions["chart-3c"].boxout}</div></aside>`
    - Wrap key terms in editorial prose (caption + boxout — but Markdown evaluates FileAttachment-loaded strings as raw text; `<abbr>` markup MUST live in the page itself, not be parsed from the JSON value). So the page hand-writes `<abbr data-glossary="cfd">CfDs</abbr>` etc. in any prose paragraphs that are written directly in the .md file. The boxout text from captions.json is plain text — rendering it via `${...}` does not produce abbr tags. Two options:
      - (a) Put boxout text directly in the .md file (editorial prose lives in the page, not the JSON), and write abbr tags inline.
      - (b) Keep boxout in JSON but also add a "Key terms" line directly in the .md page that uses abbr markup, ensuring at least some abbr usage.
      - Prefer (a) for the scissors page since the boxout is canonical editorial — but check captions.json: if Phase 01 already moved boxout text to JSON, leave it there for now and add abbr-marked prose elsewhere on the page (e.g. an "About this chart" intro paragraph above the figure).

      Recommended: add a short intro paragraph just below the h1, BEFORE the chart figure:
      ```html
      <p class="text-base text-primary leading-relaxed mb-6">
        Under the UK's <abbr data-glossary="cfd">CfD</abbr> scheme, generators are paid the difference between an agreed
        <abbr data-glossary="strike_price">strike price</abbr> and the wholesale market
        <abbr data-glossary="reference_price">reference price</abbr> (specifically the
        <abbr data-glossary="imrp">IMRP</abbr> for intermittent generation).
        This chart traces those two prices over time across all
        <abbr data-glossary="allocation_round">allocation rounds</abbr>.
      </p>
      ```
      Five abbr tags using five of the seven slugs — satisfies D-20 and gives test_glossary_refs.py real coverage.

    **Plot dark-theme re-skin** (per PATTERNS.md scissors.md section):
    - Replace `var(--okabe-blue)` / `var(--okabe-orange)` / `var(--okabe-vermillion)` / `var(--okabe-green)` with literal hex values:
      - strike line: `"#58a6ff"` (was okabe-blue)
      - market line: `"#e6a817"` (was okabe-orange)
      - subsidy area fill: `"#f44336"` at `0.20` opacity (was okabe-vermillion)
      - clawback area fill: `"#4caf50"` at `0.20` opacity (was okabe-green)
    - Add to Plot spec (top-level options): `style: { color: "#e6edf3", background: "#0d1117", fontSize: "14px" }`
    - Adjust gridline color in marks if explicit: `stroke: "#21262d"`.

    Add at end of file:
    ```html
    <script type="module" src="/client/glossary-tooltip.js"></script>
    ```

    **Mitigation note**: The Download Image anchor uses a plain `<a download>` — no JS click handler. Phase 01 UI-SPEC contract preserved. Filename includes `${meta.max_settlement_date}` so dated downloads don't collide.

    Build and verify:
    ```
    npm run build
    grep -q 'chart-3c.png' dist/charts/scissors/index.html || grep -q 'chart-3c.png' dist/charts/scissors.html
    ```
  </action>
  <acceptance_criteria>
    - `grep -q 'FileAttachment.*chart-3c.json' src/charts/scissors.md` (data wiring preserved)
    - `grep -q 'FileAttachment.*meta.json' src/charts/scissors.md`
    - `grep -q 'FileAttachment.*captions.json' src/charts/scissors.md`
    - `grep -q '/assets/charts/chart-3c.png' src/charts/scissors.md` (Download Image present)
    - `grep -q 'download="cfd-scissors' src/charts/scissors.md` (dated filename)
    - `grep -q '/client/glossary-tooltip.js' src/charts/scissors.md`
    - `grep -q 'data-glossary="cfd"' src/charts/scissors.md` (at least one abbr present)
    - `grep -cE 'data-glossary="(strike_price|reference_price|imrp|allocation_round)"' src/charts/scissors.md` ≥ 3 (multiple slugs used)
    - `grep -q '#58a6ff' src/charts/scissors.md` (dark-theme strike colour)
    - `grep -q '#e6a817' src/charts/scissors.md` (dark-theme market colour)
    - `grep -q '#0d1117' src/charts/scissors.md` (dark background in Plot style)
    - `! grep -q 'okabe-' src/charts/scissors.md` (Phase 01 light-theme tokens removed)
    - `grep -q 'role="img"' src/charts/scissors.md` (figure ARIA preserved)
    - `grep -q 'min-h-\[44px\]' src/charts/scissors.md` (44px tap target on download anchors)
    - `grep -q '← All charts' src/charts/scissors.md` (breadcrumb)
    - Build: `npm run build` exits 0
    - dist contains scissors page: `(test -f dist/charts/scissors/index.html || test -f dist/charts/scissors.html)` exits 0
    - Page contains chart-3c.png reference: `grep -q chart-3c.png dist/charts/scissors/index.html 2>/dev/null || grep -q chart-3c.png dist/charts/scissors.html`
    - test_glossary_refs.py still GREEN (all referenced slugs are defined)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && (test -f dist/charts/scissors/index.html || test -f dist/charts/scissors.html) && uv run pytest tests/test_glossary_refs.py -x</automated>
  </verify>
  <done>
    scissors.md: data-loader wiring preserved (3 FileAttachment lines + chartDataUrl); Plot re-skinned to dark-theme tokens (no okabe vars); breadcrumb + h1 + caption + intro-with-abbrs + figure + source line + download strip (JSON + Image, both with min-h 44px and dated filename) + boxout + glossary-tooltip script tag. Build succeeds; scissors detail page in dist/. test_glossary_refs.py GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static markdown → public HTML | Content authored in repo; no user input; XSS surface bounded to authored content |
| meta.json data attributes → DOM | numeric values rendered into data-* attributes via Framework templating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-02 | T (Tampering) — stale-data deception | src/index.md hero — "As of" stamp + label | mitigate | "As of" uses meta.ytd_as_of (data date); year label uses meta.ytd_label_year (derived from data date in build_meta plan 03). Acceptance criteria assert presence of both. |
| T-01.1-16 | E (Elevation) — broken card link routes user to 404 | src/index.md card hrefs | mitigate | Card hrefs hardcoded to the 4 routes registered in observablehq.config.js (plan 02). plan 09 smoke asserts all 4 detail pages exist in dist/. |
| T-01.1-17 | I (Information disclosure) — raw error in page | both pages — Plot may throw on missing data | accept | Phase 01 empty/error-state copy already locks user-visible messaging. Framework reactive cells surface failures to console only by default; not rendered into the page DOM. |
</threat_model>

<verification>
- `npm run build` exits 0 (full build incl. CSS).
- dist/index.html exists, contains shock-numeral, contains all 4 card hrefs, contains both client script tags.
- dist/charts/scissors/{index.html or scissors.html} exists, references chart-3c.png + chart-3c.json downloads, includes glossary-tooltip script tag, Plot uses dark hex tokens.
- test_glossary_refs.py GREEN (abbr slugs all defined).
- Manual visual sanity: `npm run dev` (or `npx @observablehq/framework preview`) and load http://localhost:3000 — confirm dark theme, card grid, ticking shock counter, scissors page renders dark.
</verification>

<success_criteria>
- src/index.md: full landing page with hero counter + 4 cards + footer, dark Tailwind throughout, both client modules loaded
- src/charts/scissors.md: data wiring preserved, dark-theme Plot, Download Image anchor with dated filename, glossary abbrs in editorial prose
- Build succeeds; dist contains both pages with expected markers
- Plan 08 (placeholder pages) and plan 09 (smoke) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-07-SUMMARY.md`
</output>
