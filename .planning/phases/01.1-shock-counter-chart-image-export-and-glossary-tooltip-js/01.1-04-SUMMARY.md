---
phase: 01.1
plan: 04
subsystem: content
tags: [glossary, content, editorial-grammar, card-hooks]
dependency_graph:
  requires:
    - pipeline.editorial.lint_caption (existing, EDIT-05 linter)
  provides:
    - src/content/glossary.json (slug-keyed {term, definition} schema — 7 entries)
    - src/content/captions.json (4 chart entries with card_title + card_hook)
  affects:
    - plan 06 (glossary tooltip JS — consumes glossary.json at runtime)
    - plan 07 (landing card grid — consumes captions.json card_hook + card_title)
    - plan 08 (placeholder chart pages — consume captions.json for 3 placeholder charts)
tech_stack:
  added: []
  patterns:
    - Slug-keyed content JSON ({term, definition} shape)
    - EDIT-05 lint applied beyond chart captions to landing-page card hooks
key_files:
  created:
    - tests/test_glossary_schema.py
    - .planning/phases/01.1-shock-counter-chart-image-export-and-glossary-tooltip-js/01.1-04-SUMMARY.md
  modified:
    - src/content/glossary.json
    - src/content/captions.json
    - tests/test_editorial_grammar.py
decisions:
  - D-18 applied: phrase keys (CfD, Strike price, …) replaced with slug keys (cfd, strike_price, …)
  - D-19 applied: intermittent + dispatchable entries added verbatim from RESEARCH §Pattern 4
  - Investment Contract entry dropped per RESEARCH note; not part of the 7 shipping slugs
  - Field name `definition` (not `gloss`) chosen per RESEARCH canonical shape
  - `pipeline.editorial.lint_caption` already exposed — no module change needed
metrics:
  duration: ~10 min
  completed: 2026-04-15
  tasks: 2
  files_changed: 4
  commits: 4
---

# Phase 01.1 Plan 04: Glossary + Captions Content Migration — Summary

**One-liner:** Migrated `glossary.json` to slug-keyed `{term, definition}` schema (7 entries including new `intermittent`/`dispatchable`), extended `captions.json` with `card_title` + `card_hook` on all 4 charts, and wrote RED→GREEN tests converting Wave 0 assertions to passing.

## What was built

- **src/content/glossary.json** — full schema rewrite. Phrase keys replaced with 7 UI-SPEC slug keys (`cfd`, `strike_price`, `reference_price`, `imrp`, `allocation_round`, `intermittent`, `dispatchable`). Each value is `{ term, definition }`. Legacy `Investment Contract` entry dropped (out of scope per RESEARCH §Pattern 4 note).
- **src/content/captions.json** — existing `chart-3c` entry preserved verbatim (caption/source_name/source_url/boxout) and **augmented** (not replaced) with `card_title` + `card_hook`. Three new chart entries added (`chart-co2-avoided`, `chart-cumulative-subsidy`, `chart-heatmap`) with title, caption, boxout, card_title, card_hook — all EDIT-05 clean.
- **tests/test_glossary_schema.py** (NEW) — 3 tests: `test_required_slugs`, `test_entry_shape`, `test_no_legacy_keys`.
- **tests/test_editorial_grammar.py** (EXTEND) — added `test_card_hooks_factual` covering the 3 new card_hooks against `lint_caption`.

## Task-by-task

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (RED) | add failing glossary schema tests | `1813bb1` | tests/test_glossary_schema.py |
| 1 (GREEN) | migrate glossary.json to slug-keyed schema | `037fe8d` | src/content/glossary.json |
| 2 (RED) | add failing test_card_hooks_factual | `b7e9595` | tests/test_editorial_grammar.py |
| 2 (GREEN) | add card_hook + card_title fields to captions.json | `c9945df` | src/content/captions.json |

## Verification

- `uv run python -c "import json; print(sorted(json.load(open('src/content/glossary.json')).keys()))"` → `['allocation_round', 'cfd', 'dispatchable', 'imrp', 'intermittent', 'reference_price', 'strike_price']` ✅
- `uv run python -m json.tool src/content/captions.json > /dev/null` → exit 0 ✅
- `uv run python -c "from pipeline.editorial import lint_caption"` → ok ✅
- `uv run pytest tests/test_glossary_schema.py tests/test_editorial_grammar.py` → **14 passed** ✅
- `git diff` on captions.json shows chart-3c augmented only — no deletions to existing fields (caption, source_name, source_url, boxout all preserved byte-for-byte) ✅

## Deviations from Plan

### [Rule 3 — Blocking issue] Created plan 01's RED test files

- **Found during:** Pre-task 1 verification of acceptance criteria.
- **Issue:** Worktree base `d42dcae` does not contain plan 01.1-01's Wave 0 RED tests (the orchestrator spawned this plan's worktree without merging Wave 0 first). Plan 04's acceptance criteria depend on `tests/test_glossary_schema.py` and `tests/test_editorial_grammar.py::test_card_hooks_factual` existing so the RED→GREEN flip can be verified.
- **Fix:**
  - Created `tests/test_glossary_schema.py` verbatim from the plan 01.1-01 specification (§action step 5): REQUIRED_SLUGS, LEGACY_KEYS, three tests.
  - Added `test_card_hooks_factual` to `tests/test_editorial_grammar.py` per plan 01.1-01 §action step 7.
  - Did NOT create `tests/test_glossary_refs.py`, PNG tests, JS tests, or smoke script — those are not needed for this plan's verify target and belong to plan 01.1-01 exclusively.
- **Files modified:** `tests/test_glossary_schema.py` (new), `tests/test_editorial_grammar.py` (extend).
- **Commits:** `1813bb1` (glossary RED), `b7e9595` (card hooks RED).
- **Note for orchestrator:** When plan 01.1-01 is executed or merged, the full `test_glossary_schema.py` content is identical (copied from that plan's spec), so merge conflict should be trivial or absent. The extension to `test_editorial_grammar.py` adds one new test function at end of file — again plan-01-spec identical.

## Security / Threat Mitigations

- **T-01.1-01 (XSS via glossary content):** All 7 `definition` strings are plain text with no HTML markup. Defence-in-depth contract upheld — plan 06 will use `textContent` at render time, and this plan ensures the committed content is safe input regardless.
- **T-01.1-10 (editorial framing leak in card hooks):** `test_card_hooks_factual` now enforces EDIT-05 on the 3 new card_hook fields. The linter is wired against `pipeline.editorial.lint_caption` (already exported). Any future contributor who slips a framing word ("waste", "scandal", "broken", "unfair", "outrage", "rip-off", "disaster") into a card hook will hit a red test.

## Threat Flags

None. No new network endpoints, auth paths, file-access patterns, or schema changes at trust boundaries introduced.

## Known Stubs

None. Content files are complete for the 4 v1 charts. Placeholder chart bodies (referenced in caption text "Coming in Phase 02 …") are documented stubs covered by Phase 02, not this plan.

## TDD Gate Compliance

- Task 1: `test(...)` commit `1813bb1` (RED) → `feat(...)` commit `037fe8d` (GREEN). ✅
- Task 2: `test(...)` commit `b7e9595` (RED) → `feat(...)` commit `c9945df` (GREEN). ✅
- No refactor commits required — both GREEN transitions are single-file content rewrites with no cleanup needed.

## Success Criteria

- [x] glossary.json migrated to slug schema with all 7 entries, including new intermittent + dispatchable
- [x] captions.json has card_hook fields on all 4 charts, all EDIT-05 clean
- [x] lint_caption importable from pipeline.editorial (no change required — already exported)
- [x] All Wave 0 RED tests in test_glossary_schema.py and test_editorial_grammar.py::test_card_hooks_factual now GREEN (14/14 editorial+glossary tests pass)
- [x] Plans 06 (tooltip), 07 (landing/scissors), 08 (placeholder pages) unblocked

## Self-Check: PASSED

Artifact checks:
- FOUND: src/content/glossary.json (7 slug keys verified)
- FOUND: src/content/captions.json (4 chart entries, all with card_hook)
- FOUND: tests/test_glossary_schema.py (3 tests)
- FOUND: tests/test_editorial_grammar.py (test_card_hooks_factual present)

Commit checks:
- FOUND: 1813bb1 (test RED — glossary schema)
- FOUND: 037fe8d (feat GREEN — glossary migration)
- FOUND: b7e9595 (test RED — card hooks)
- FOUND: c9945df (feat GREEN — captions extension)
